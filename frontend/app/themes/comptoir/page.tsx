import ComptoirTheme from "@/themes/ComptoirTheme";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "FiscalFlow · Comptoir" };

export default function ComptoirPage() {
  return <ComptoirTheme />;
}
