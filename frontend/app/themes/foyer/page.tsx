import FoyerTheme from "@/themes/FoyerTheme";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "FiscalFlow · Foyer" };

export default function FoyerPage() {
  return <FoyerTheme />;
}
