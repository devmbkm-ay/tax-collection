import AtlasTheme from "@/themes/AtlasTheme";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "FiscalFlow · Atlas" };

export default function AtlasPage() {
  return <AtlasTheme />;
}
